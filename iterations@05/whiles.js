// while (condition) 
// {
    
// }

let i, j;
i = 0;
while(i <= 10)
{
    // console.log(`${i}`);
    i += 2;
}

let myarr = ["h", "e", "t", "d", "m", "hehe"];
i = 0;
while(i < myarr.length)
{
    // console.log(myarr[i]);
    i++;
}


// do {
    
// } while (condition);
i = 1;
do 
{
    console.log(`score is ${i}`);
    i++;
} 
while (i <= 10);
// if instead of 1 the value of i is set to be greater than 10 then also the loop will run 1 time this says that 
// regardless of while condition the loop executes at least once
